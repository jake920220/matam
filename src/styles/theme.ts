const light = {
    bgColor: "#fff",
    bodyColor: "#f5f5f5",
    textColor: "#000",
};
const dark = {
    bgColor: "#121212",
    textColor: "#ECECEC",
};

const theme = {
    light,
    dark,
};

export default theme;
